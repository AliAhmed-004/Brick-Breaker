// PADDLE
import 'package:flutter/material.dart';

const Color paddleColor = Colors.purpleAccent;
const double paddleWidth = 100;
const double paddleHeight = 10;
const double paddleMovementSpeed = 300;

// BALL
Color ballColor = Colors.purple[200]!;
const double ballRadius = 10;
const double ballVelocity = 300;
